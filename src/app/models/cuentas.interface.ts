export interface cuentas {
    fecha: any[],
    registro: any[]
}

export interface promedio_custom{

    fecha: string[],
    maximos: number[],
    minimos: number[],
    promedios: number[]
  }
  
